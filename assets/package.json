{
  "name": "@zhortein/seo-tracking-bundle",
  "version": "1.7.0",
  "symfony": {
    "controllers": {
      "tracking": {
        "main": "dist/tracking_controller.js",
        "fetch": "lazy",
        "enabled": true
      }
    },
    "importmap": {
      "@hotwired/stimulus": "^3.0.0"
    }
  },
  "peerDependencies": {
    "@hotwired/stimulus": "^3.0.0"
  },
  "scripts": {
    "lint": "node --check dist/tracking_controller.js",
    "test": "node --test tests/*.test.mjs"
  }
}
